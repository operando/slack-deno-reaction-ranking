import {PopulatedArray} from "https://deno.land/x/deno_slack_api@1.0.1/type-helpers.ts";

export const CHANNEL_IDS: PopulatedArray<string> = ["XXXX"]; // Listing all the channels to enable here is required
export const SLACK_API_TOKEN: string = "XXXX"; // Set User OAuth Token
